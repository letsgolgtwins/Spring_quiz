<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quiz.retry.lesson02.mapper.StoreMapperRetry">
 	<select id="selectStoreRetryList" resultType="com.quiz.retry.lesson02.domain.StoreRetry">
 		SELECT
 			*
 		FROM
 		`store`
 	</select>
 </mapper>